{% assign is_zh = false %}
{% if page.lang contains 'zh' %}{% assign is_zh = true %}{% endif %}
<div class="resource-directory">
  {% for resource in site.data.resources %}
    <section class="resource-entry">
      <div class="resource-entry-meta">
        <span>0{{ forloop.index }}</span>
        <span>{% if is_zh %}{{ resource.type_zh }}{% else %}{{ resource.type_en }}{% endif %}</span>
      </div>
      {% if resource.image %}
        <figure class="resource-entry-media">
          <img
            src="{{ resource.image | relative_url }}"
            alt="{% if is_zh %}{{ resource.image_alt_zh }}{% else %}{{ resource.image_alt_en }}{% endif %}"
            width="960"
            height="600"
            loading="lazy"
          >
        </figure>
      {% endif %}
      <div class="resource-entry-body">
        <h2>{{ resource.name }}</h2>
        <p>{% if is_zh %}{{ resource.description_zh }}{% else %}{{ resource.description_en }}{% endif %}</p>
        <div class="resource-entry-links">
          {% for link in resource.links %}
            <a href="{{ link.url | relative_url }}" target="_blank" rel="noopener noreferrer">
              {% if is_zh %}{{ link.label_zh }}{% else %}{{ link.label }}{% endif %} <span aria-hidden="true">↗</span>
            </a>
          {% endfor %}
        </div>
      </div>
    </section>
  {% endfor %}
</div>
{% unless include.embedded %}
  <aside class="resource-note">
    {% if is_zh %}
      <strong>资源说明</strong>
      <p>本页列出与研究方向相关的代码、数据、教程和系统材料。</p>
    {% else %}
      <strong>About these resources</strong>
      <p>This page lists systems, codebases, datasets, and tutorials associated with the research agenda.</p>
    {% endif %}
  </aside>
{% endunless %}
